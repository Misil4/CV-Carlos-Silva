/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains : ['raw.githubusercontent.com','github.com','camo.githubusercontent.com','3.bp.blogspot.com']
  }
}

module.exports = nextConfig
